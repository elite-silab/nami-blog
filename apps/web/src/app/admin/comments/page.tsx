"use client";
import Link from "next/link";
import { useSearchParams } from "next/navigation";
import { Suspense, useCallback, useEffect, useState } from "react";
import { adminFetch } from "@/lib/admin-session";
import { readJson } from "@/lib/json";
type Comment = { id: number; author_name: string; author_email?: string; content: string; status: string; created_at: string; post_title: string };
type CommentResult = { data: Comment[]; meta: { total: number; limit: number } };
function CommentList() { const params = useSearchParams(); const tab = params?.get("tab") || "all"; const page = Number(params?.get("page") || 1); const [result, setResult] = useState<CommentResult | null>(null); const load = useCallback(() => { const query = new URLSearchParams({ page: String(page) }); if (tab !== "all") query.set("status", tab); adminFetch(`/api/admin/comments?${query}`).then((response) => readJson<CommentResult>(response)).then(setResult); }, [page, tab]); useEffect(load, [load]); async function act(id: number, action: "approved" | "rejected" | "delete") { if (action === "delete" && !confirm("确认删除这条评论？")) return; const response = await adminFetch(action === "delete" ? `/api/admin/comments/${id}` : `/api/admin/comments/${id}/status`, { method: action === "delete" ? "DELETE" : "PATCH", headers: { "Content-Type": "application/json" }, body: action === "delete" ? undefined : JSON.stringify({ status: action }) }); if (!response.ok) return alert("操作失败"); load(); } const pages = Math.ceil((result?.meta.total || 0) / (result?.meta.limit || 20)); return <div><div className="flex gap-2">{[["all", "全部"], ["pending", "待审核"], ["approved", "已批准"]].map(([value, label]) => <Link key={value} href={value === "all" ? "/admin/comments" : `/admin/comments?tab=${value}`} className={`rounded-lg px-3 py-1.5 text-sm ${tab === value ? "bg-[var(--color-primary)] text-white" : "text-[var(--color-text-secondary)]"}`}>{label}</Link>)}</div><div className="mt-4 space-y-3">{!result ? <p className="py-12 text-center">加载中…</p> : result.data.length ? result.data.map((comment) => <article key={comment.id} className="rounded-xl border border-[var(--color-border)] bg-[var(--color-bg)] p-4"><div className="flex justify-between"><div><strong>{comment.author_name}</strong><span className="ml-2 rounded-full bg-[var(--color-bg-tertiary)] px-2 py-0.5 text-xs">{comment.status}</span></div><time className="text-xs text-[var(--color-text-tertiary)]">{new Date(comment.created_at).toLocaleString("zh-CN")}</time></div><p className="mt-2 text-sm">{comment.content}</p><div className="mt-3 flex justify-between text-xs"><span>文章：<span className="text-[var(--color-primary)]">{comment.post_title}</span></span><div className="flex gap-3">{comment.status !== "approved" && <button onClick={() => void act(comment.id, "approved")} className="text-[var(--color-success)]">批准</button>}{comment.status !== "rejected" && <button onClick={() => void act(comment.id, "rejected")} className="text-[var(--color-warning)]">拒绝</button>}<button onClick={() => void act(comment.id, "delete")} className="text-[var(--color-danger)]">删除</button></div></div></article>) : <p className="rounded-xl border bg-[var(--color-bg)] py-12 text-center">暂无评论</p>}</div>{result && pages > 1 && <div className="mt-4 flex justify-between text-sm"><span>共 {result.meta.total} 条，第 {page}/{pages} 页</span><div>{page > 1 && <Link href={`/admin/comments?page=${page - 1}&tab=${tab}`}>上一页</Link>} {page < pages && <Link href={`/admin/comments?page=${page + 1}&tab=${tab}`}>下一页</Link>}</div></div>}</div>; }
export default function CommentsPage() { return <Suspense fallback={<p>加载中…</p>}><CommentList /></Suspense>; }
