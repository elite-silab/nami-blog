"use client";
import Link from "next/link";
import { useEffect, useState } from "react";
import { adminFetch } from "@/lib/admin-session";
import { readJson } from "@/lib/json";
type Dashboard = { posts: { published: number; draft: number }; comments: { total: number; pending: number }; views: { total: number }; taxonomies: { categories: number; tags: number }; recentPosts: Array<{ id: number; title: string; status: string; updated_at: string }>; recentComments: Array<{ id: number; author_name: string; content: string; post_title: string }> };
export default function DashboardPage() { const [data, setData] = useState<Dashboard | null>(null); useEffect(() => { adminFetch("/api/admin/dashboard").then((response) => readJson<{ data: Dashboard }>(response)).then((result) => setData(result.data)).catch(() => {}); }, []); if (!data) return <p className="py-12 text-center text-sm text-[var(--color-text-tertiary)]">加载中…</p>; const cards = [["文章总数", data.posts.published + data.posts.draft, `已发布 ${data.posts.published} · 草稿 ${data.posts.draft}`], ["评论总数", data.comments.total, `${data.comments.pending} 条待审核`], ["总浏览量", data.views.total.toLocaleString("zh-CN"), "公开文章累计阅读"], ["分类与标签", data.taxonomies.categories + data.taxonomies.tags, `${data.taxonomies.categories} 个分类 · ${data.taxonomies.tags} 个标签`]]; return <div className="space-y-8"><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{cards.map(([title, value, note]) => <div key={title} className="rounded-xl border border-[var(--color-border)] bg-[var(--color-bg)] p-5"><p className="text-sm text-[var(--color-text-secondary)]">{title}</p><p className="mt-1 text-3xl font-bold">{value}</p><p className="mt-2 text-xs text-[var(--color-text-tertiary)]">{note}</p></div>)}</div><div><h2 className="text-lg font-semibold">快捷操作</h2><div className="mt-4 grid gap-4 sm:grid-cols-3">{[["/admin/posts/new", "📝", "写新文章"], ["/admin/comments", "💬", "审核评论"], ["/admin/friends", "🔗", "友链管理"]].map(([href, icon, label]) => <Link key={href} href={href} className="flex items-center gap-3 rounded-xl border border-[var(--color-border)] bg-[var(--color-bg)] p-4 hover:border-[var(--color-primary)]"><span className="text-2xl">{icon}</span><strong>{label}</strong></Link>)}</div></div><div className="grid gap-6 lg:grid-cols-2"><section className="rounded-xl border border-[var(--color-border)] bg-[var(--color-bg)]"><h3 className="border-b border-[var(--color-border)] px-5 py-4 font-semibold">最近文章</h3><div className="divide-y divide-[var(--color-border)]">{data.recentPosts.length ? data.recentPosts.map((post) => <div key={post.id} className="flex justify-between px-5 py-3 text-sm"><span>{post.title}</span><time className="text-xs text-[var(--color-text-tertiary)]">{new Date(post.updated_at).toLocaleDateString("zh-CN")}</time></div>) : <p className="px-5 py-8 text-center text-sm">暂无文章</p>}</div></section><section className="rounded-xl border border-[var(--color-border)] bg-[var(--color-bg)]"><h3 className="border-b border-[var(--color-border)] px-5 py-4 font-semibold">待审核评论</h3><div className="divide-y divide-[var(--color-border)]">{data.recentComments.length ? data.recentComments.map((comment) => <div key={comment.id} className="px-5 py-3 text-sm"><p><strong>{comment.author_name}</strong> 在 <span className="text-[var(--color-primary)]">{comment.post_title}</span></p><p className="mt-1 line-clamp-2 text-[var(--color-text-secondary)]">{comment.content}</p></div>) : <p className="px-5 py-8 text-center text-sm">暂无待审核评论</p>}</div></section></div></div>; }
