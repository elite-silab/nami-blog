"use client";
import { FormEvent, useCallback, useEffect, useState } from "react";
import { adminFetch } from "@/lib/admin-session";
import { publicationMessage } from "@/lib/publication";
import { readJson, type ApiErrorResult } from "@/lib/json";

type Kind = "categories" | "tags" | "friends";
type Row = Record<string, unknown> & { id: number; name: string; slug?: string; description?: string; post_count?: number; color?: string; url?: string; avatar_url?: string; sort_order?: number };
const labels = { categories: "分类", tags: "标签", friends: "友链" };
const defaults = { categories: { name: "", slug: "", description: "", sort_order: 0 }, tags: { name: "", slug: "", color: "#3b82f6" }, friends: { name: "", url: "", avatar_url: "", description: "" } };
type MutationResult = { data?: { publication?: { status?: string } } } & ApiErrorResult;

export function ResourceManager({ kind }: { kind: Kind }) {
  const [rows, setRows] = useState<Row[] | null>(null); const [editing, setEditing] = useState<Partial<Row> | null>(null); const [notice, setNotice] = useState("");
  const load = useCallback(async () => { const response = await adminFetch(`/api/admin/${kind}`); const result = await readJson<{ data?: Row[] }>(response); setRows(response.ok ? result.data || [] : []); }, [kind]);
  useEffect(() => { void load(); }, [load]);
  async function save(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const form = new FormData(event.currentTarget); const payload: Record<string, unknown> = {}; for (const [key, value] of form.entries()) payload[key] = key === "sort_order" ? Number(value) || 0 : value || null; const response = await adminFetch(`/api/admin/${kind}${editing?.id ? `/${editing.id}` : ""}`, { method: editing?.id ? "PUT" : "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(payload) }); const result = await readJson<MutationResult>(response); if (!response.ok) return alert(result.error?.message || "保存失败"); setNotice(publicationMessage(result.data?.publication?.status)); setEditing(null); await load(); }
  async function remove(row: Row) { if (kind === "categories" && Number(row.post_count) > 0) return alert(`分类「${row.name}」下仍有文章，请先迁移文章`); if (!confirm(`确认删除${labels[kind]}「${row.name}」？`)) return; const response = await adminFetch(`/api/admin/${kind}/${row.id}`, { method: "DELETE" }); const result = await readJson<MutationResult>(response); if (!response.ok) return alert(result.error?.message || "删除失败"); setNotice(publicationMessage(result.data?.publication?.status)); await load(); }
  const field = "mt-1 w-full rounded-lg border border-[var(--color-border)] bg-[var(--color-bg)] px-3 py-2 text-sm";
  return <div><div className="flex items-center justify-between"><div><h2 className="text-lg font-semibold">{labels[kind]}列表</h2>{notice && <p className="mt-1 text-sm text-[var(--color-success)]">✓ {notice}</p>}</div><button type="button" onClick={() => setEditing(defaults[kind])} className="rounded-lg bg-[var(--color-primary)] px-4 py-2 text-sm font-medium text-white">+ 新建{labels[kind]}</button></div><div className="mt-4 overflow-x-auto rounded-xl border border-[var(--color-border)] bg-[var(--color-bg)]">{rows === null ? <p className="py-12 text-center text-sm text-[var(--color-text-tertiary)]">加载中…</p> : rows.length === 0 ? <p className="py-12 text-center text-[var(--color-text-tertiary)]">暂无{labels[kind]}</p> : <table className="w-full text-sm"><thead className="border-b border-[var(--color-border)] bg-[var(--color-bg-secondary)]"><tr><th className="px-4 py-3 text-left">名称</th><th className="px-4 py-3 text-left">{kind === "friends" ? "URL" : "Slug"}</th><th className="px-4 py-3 text-left">{kind === "tags" ? "颜色" : "描述"}</th>{kind !== "friends" && <th className="px-4 py-3 text-center">文章数</th>}<th className="px-4 py-3 text-right">操作</th></tr></thead><tbody className="divide-y divide-[var(--color-border)]">{rows.map((row) => <tr key={row.id}><td className="px-4 py-3 font-medium">{row.name}</td><td className="px-4 py-3 text-[var(--color-text-secondary)]">{kind === "friends" ? row.url : row.slug}</td><td className="px-4 py-3 text-[var(--color-text-secondary)]">{kind === "tags" ? <span className="inline-flex items-center gap-2"><i className="h-3 w-3 rounded-full" style={{ background: row.color || "#3b82f6" }} />{row.color}</span> : row.description || "—"}</td>{kind !== "friends" && <td className="px-4 py-3 text-center">{row.post_count || 0}</td>}<td className="px-4 py-3 text-right"><button onClick={() => setEditing(row)} className="text-[var(--color-primary)]">编辑</button><button onClick={() => void remove(row)} className="ml-3 text-[var(--color-danger)]">删除</button></td></tr>)}</tbody></table>}</div>{editing && <div className="fixed inset-0 z-[80] grid place-items-center bg-black/45 p-4" role="presentation" onMouseDown={(event) => { if (event.target === event.currentTarget) setEditing(null); }}><form onSubmit={save} className="w-full max-w-lg space-y-5 rounded-2xl border border-[var(--color-border)] bg-[var(--color-bg)] p-6 shadow-2xl"><div className="flex items-start justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.16em] text-[var(--color-primary)]">Content manager</p><h3 className="mt-1 text-xl font-semibold">{editing.id ? "编辑" : "新建"}{labels[kind]}</h3></div><button type="button" onClick={() => setEditing(null)} className="grid h-9 w-9 place-items-center rounded-full border border-[var(--color-border)] text-xl" aria-label="关闭">×</button></div><label className="block text-sm font-medium">名称 *<input name="name" required defaultValue={String(editing.name || "")} autoFocus className={field} /></label>{kind !== "friends" && <label className="block text-sm font-medium">Slug *<input name="slug" required defaultValue={String(editing.slug || "")} className={field} /><small className="text-[var(--color-text-tertiary)]">使用小写英文和连字符，例如 dev-log</small></label>}{kind === "categories" && <><label className="block text-sm font-medium">描述<textarea name="description" rows={2} defaultValue={String(editing.description || "")} className={field} /></label><label className="block text-sm font-medium">排序权重<input name="sort_order" type="number" defaultValue={Number(editing.sort_order || 0)} className={field} /></label></>}{kind === "tags" && <label className="block text-sm font-medium">颜色<input name="color" type="color" defaultValue={String(editing.color || "#3b82f6")} className={`${field} h-11`} /></label>}{kind === "friends" && <><label className="block text-sm font-medium">URL *<input name="url" type="url" required defaultValue={String(editing.url || "")} className={field} /></label><label className="block text-sm font-medium">头像 URL<input name="avatar_url" type="url" defaultValue={String(editing.avatar_url || "")} className={field} /></label><label className="block text-sm font-medium">描述<textarea name="description" rows={2} defaultValue={String(editing.description || "")} className={field} /></label></>}<div className="flex justify-end gap-2"><button type="button" onClick={() => setEditing(null)} className="rounded-lg border px-4 py-2 text-sm">取消</button><button className="rounded-lg bg-[var(--color-primary)] px-4 py-2 text-sm font-medium text-white">保存</button></div></form></div>}</div>;
}
