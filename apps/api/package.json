{
  "name": "@nami/api",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint src/",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "db:migrate": "CI=true wrangler d1 migrations apply nami-blog --local --config ../../wrangler.jsonc",
    "db:seed": "node scripts/seed.mjs",
    "db:reset": "wrangler d1 delete nami-blog --local --yes --config ../../wrangler.jsonc 2>/dev/null; wrangler d1 create nami-blog --local --config ../../wrangler.jsonc && wrangler d1 migrations apply nami-blog --local --config ../../wrangler.jsonc"
  },
  "dependencies": {
    "@nami/shared": "workspace:*",
    "bcryptjs": "^3.0.3",
    "hono": "^4.7.0",
    "jose": "^6.2.4"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.18.8",
    "@cloudflare/workers-types": "^5.20260724.1",
    "@eslint/js": "^9.0.0",
    "@types/bcryptjs": "^3.0.0",
    "eslint": "^9.0.0",
    "typescript": "^5.8.0",
    "typescript-eslint": "^8.0.0",
    "vitest": "^4.1.10",
    "wrangler": "^4.0.0"
  }
}
