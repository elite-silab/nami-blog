# 数据库设计文档

> 文档版本：v2.1
> 更新日期：2026-07-25
> 适用范围：Nami 娜美 CF 博客  
> 项目状态：**已初始化，公开读模型与管理员写模型已落地**
> 数据库引擎：**Cloudflare D1 (SQLite)**  
> 迁移工具：**D1 内置迁移 (wrangler d1 migrations apply)**

---

> **适合谁读**：需要理解数据结构、编写数据库迁移、或开发新功能的贡献者。

## 1. 设计原则

- **命名**：表名和列名统一 `snake_case`，复数表名（`posts`、`comments`）
- **主键**：`INTEGER PRIMARY KEY AUTOINCREMENT`
- **时间**：统一 `TEXT` 存储 ISO 8601 格式（`datetime('now')`），列名 `created_at` / `updated_at`，由应用层写入
- **布尔值**：`INTEGER`（0 = false, 1 = true），SQLite 无原生 BOOLEAN
- **JSON**：`TEXT` 存储 JSON 字符串，应用层序列化/反序列化
- **软删除**：关键内容表使用 `deleted_at TEXT DEFAULT NULL`
- **索引命名**：`idx_<table>_<columns>`，唯一索引 `uk_<table>_<columns>`
- **枚举**：使用 `CHECK` 约束 + 应用层校验
- **迁移**：每次变更生成 `NNNN_description.sql`，已进入远程环境的 migration 永不修改
- **D1 限制**：不支持 `ALTER TABLE DROP COLUMN`（部分版本），删除列需重建表；部分索引 `WHERE` 子句支持有限（如不支持 `deleted_at IS NULL` 的部分版本需回退到全表索引）

---

## 2. ER 关系概览

```
users 1 ── N posts
posts N ── N categories  （通过 post_categories 中间表）
posts N ── N tags        （通过 post_tags 中间表）
posts 1 ── N comments

users 1 ── N refresh_tokens
users 1 ── N audit_logs

categories 1 ── N categories（自引用，parent_id 实现树形）

friends（友链，独立表）
site_settings（站点配置，KV 结构）

> **图片存储**：图片直接存放在仓库 `public/images/` 目录，通过 Cloudflare Pages 作为静态资源分发，
> 文章正文中使用相对路径引用（如 `/images/xxx.png`），无需独立对象存储。
```

---

## 3. 表结构详细设计

> 以下 SQL 均为 D1 (SQLite) 兼容语法，与 `migrations/0001_init_schema.sql` 保持一致。

### 3.1 users（用户 / 博主）

MVP 仅单博主，预留扩展。

```sql
CREATE TABLE IF NOT EXISTS users (
    id            INTEGER PRIMARY KEY AUTOINCREMENT,
    username      TEXT NOT NULL,
    email         TEXT NOT NULL,
    password_hash TEXT NOT NULL,
    display_name  TEXT,
    avatar_url    TEXT,
    bio           TEXT,
    role          TEXT NOT NULL DEFAULT 'admin' CHECK (role IN ('admin')),
    status        TEXT NOT NULL DEFAULT 'active' CHECK (status IN ('active', 'disabled')),
    created_at    TEXT NOT NULL DEFAULT (datetime('now')),
    updated_at    TEXT NOT NULL DEFAULT (datetime('now')),
    deleted_at    TEXT
);

CREATE UNIQUE INDEX IF NOT EXISTS uk_users_username ON users(username) WHERE deleted_at IS NULL;
CREATE UNIQUE INDEX IF NOT EXISTS uk_users_email    ON users(email)    WHERE deleted_at IS NULL;
```

### 3.2 posts（文章）

```sql
CREATE TABLE IF NOT EXISTS posts (
    id            INTEGER PRIMARY KEY AUTOINCREMENT,
    author_id     INTEGER NOT NULL REFERENCES users(id),
    title         TEXT NOT NULL,
    slug          TEXT NOT NULL,
    content       TEXT NOT NULL,
    content_html  TEXT,
    excerpt       TEXT,
    cover_url     TEXT,
    status        TEXT NOT NULL DEFAULT 'draft' CHECK (status IN ('draft', 'published', 'archived')),
    is_pinned     INTEGER NOT NULL DEFAULT 0,
    is_public     INTEGER NOT NULL DEFAULT 1,
    view_count    INTEGER NOT NULL DEFAULT 0,
    word_count    INTEGER NOT NULL DEFAULT 0,
    published_at  TEXT,
    created_at    TEXT NOT NULL DEFAULT (datetime('now')),
    updated_at    TEXT NOT NULL DEFAULT (datetime('now')),
    deleted_at    TEXT
);

CREATE UNIQUE INDEX IF NOT EXISTS uk_posts_slug ON posts(slug) WHERE deleted_at IS NULL;
CREATE INDEX IF NOT EXISTS idx_posts_status_published ON posts(status, published_at DESC) WHERE deleted_at IS NULL;
CREATE INDEX IF NOT EXISTS idx_posts_author ON posts(author_id);
```

### 3.3 categories（分类）

```sql
CREATE TABLE IF NOT EXISTS categories (
    id          INTEGER PRIMARY KEY AUTOINCREMENT,
    parent_id   INTEGER REFERENCES categories(id),
    name        TEXT NOT NULL,
    slug        TEXT NOT NULL,
    description TEXT,
    sort_order  INTEGER NOT NULL DEFAULT 0,
    icon        TEXT,
    created_at  TEXT NOT NULL DEFAULT (datetime('now')),
    updated_at  TEXT NOT NULL DEFAULT (datetime('now')),
    deleted_at  TEXT
);

CREATE UNIQUE INDEX IF NOT EXISTS uk_categories_slug ON categories(slug) WHERE deleted_at IS NULL;
CREATE INDEX IF NOT EXISTS idx_categories_parent ON categories(parent_id);
```

### 3.4 post_categories（文章-分类 中间表）

```sql
CREATE TABLE IF NOT EXISTS post_categories (
    post_id     INTEGER NOT NULL REFERENCES posts(id) ON DELETE CASCADE,
    category_id INTEGER NOT NULL REFERENCES categories(id) ON DELETE CASCADE,
    PRIMARY KEY (post_id, category_id)
);

CREATE INDEX IF NOT EXISTS idx_post_categories_category ON post_categories(category_id);
```

### 3.5 tags（标签）

```sql
CREATE TABLE IF NOT EXISTS tags (
    id         INTEGER PRIMARY KEY AUTOINCREMENT,
    name       TEXT NOT NULL,
    slug       TEXT NOT NULL,
    color      TEXT,
    created_at TEXT NOT NULL DEFAULT (datetime('now')),
    updated_at TEXT NOT NULL DEFAULT (datetime('now')),
    deleted_at TEXT
);

CREATE UNIQUE INDEX IF NOT EXISTS uk_tags_slug ON tags(slug) WHERE deleted_at IS NULL;
CREATE UNIQUE INDEX IF NOT EXISTS uk_tags_name ON tags(name) WHERE deleted_at IS NULL;
```

### 3.6 post_tags（文章-标签 中间表）

```sql
CREATE TABLE IF NOT EXISTS post_tags (
    post_id INTEGER NOT NULL REFERENCES posts(id) ON DELETE CASCADE,
    tag_id  INTEGER NOT NULL REFERENCES tags(id)    ON DELETE CASCADE,
    PRIMARY KEY (post_id, tag_id)
);

CREATE INDEX IF NOT EXISTS idx_post_tags_tag ON post_tags(tag_id);
```

### 3.7 comments（评论）

```sql
CREATE TABLE IF NOT EXISTS comments (
    id           INTEGER PRIMARY KEY AUTOINCREMENT,
    post_id      INTEGER NOT NULL REFERENCES posts(id) ON DELETE CASCADE,
    parent_id    INTEGER REFERENCES comments(id),
    author_name  TEXT NOT NULL,
    author_email TEXT,
    author_url   TEXT,
    content      TEXT NOT NULL,
    status       TEXT NOT NULL DEFAULT 'pending' CHECK (status IN ('pending', 'approved', 'rejected')),
    ip_address   TEXT,
    user_agent   TEXT,
    created_at   TEXT NOT NULL DEFAULT (datetime('now')),
    updated_at   TEXT NOT NULL DEFAULT (datetime('now')),
    deleted_at   TEXT
);

CREATE INDEX IF NOT EXISTS idx_comments_post   ON comments(post_id, created_at) WHERE deleted_at IS NULL;
CREATE INDEX IF NOT EXISTS idx_comments_status ON comments(status)              WHERE deleted_at IS NULL;
```

### 3.8 friends（友链）

```sql
CREATE TABLE IF NOT EXISTS friends (
    id           INTEGER PRIMARY KEY AUTOINCREMENT,
    name         TEXT NOT NULL,
    url          TEXT NOT NULL,
    avatar_url   TEXT,
    description  TEXT,
    status       TEXT NOT NULL DEFAULT 'pending' CHECK (status IN ('pending', 'approved', 'rejected')),
    sort_order   INTEGER NOT NULL DEFAULT 0,
    check_url_ok INTEGER,
    checked_at   TEXT,
    created_at   TEXT NOT NULL DEFAULT (datetime('now')),
    updated_at   TEXT NOT NULL DEFAULT (datetime('now')),
    deleted_at   TEXT
);

CREATE INDEX IF NOT EXISTS idx_friends_status ON friends(status) WHERE deleted_at IS NULL;
```

`friends.status`、`check_url_ok` 和 `checked_at` 是早期审核/检查流程的兼容字段。当前产品由管理员直接维护友链，所有未删除记录都会公开，界面不再使用这些字段。

### 3.9 refresh_tokens（刷新令牌）

```sql
CREATE TABLE IF NOT EXISTS refresh_tokens (
    id         INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id    INTEGER NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    token_hash TEXT NOT NULL,
    family     TEXT NOT NULL,
    expires_at TEXT NOT NULL,
    revoked_at TEXT,
    created_at TEXT NOT NULL DEFAULT (datetime('now'))
);

CREATE INDEX IF NOT EXISTS idx_refresh_tokens_user ON refresh_tokens(user_id);
CREATE INDEX IF NOT EXISTS idx_refresh_tokens_hash ON refresh_tokens(token_hash) WHERE revoked_at IS NULL;
```

### 3.10 audit_logs（审计日志）

```sql
CREATE TABLE IF NOT EXISTS audit_logs (
    id         INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id    INTEGER REFERENCES users(id),
    action     TEXT NOT NULL,
    target     TEXT,
    metadata   TEXT,
    ip_address TEXT,
    created_at TEXT NOT NULL DEFAULT (datetime('now'))
);

CREATE INDEX IF NOT EXISTS idx_audit_logs_action ON audit_logs(action, created_at DESC);
```

### 3.11 site_settings（站点配置）

KV 结构，value 存储 JSON 字符串。

```sql
CREATE TABLE IF NOT EXISTS site_settings (
    key        TEXT PRIMARY KEY,
    value      TEXT NOT NULL,
    updated_at TEXT NOT NULL DEFAULT (datetime('now'))
);

INSERT OR IGNORE INTO site_settings (key, value) VALUES
    ('site_name',           '"Nami Blog"'),
    ('site_subtitle',       '"记录技术与生活"'),
    ('seo_description',     '""'),
    ('social_links',        '{}'),
    ('site_theme',           '"sakura"'),
    ('comment_enabled',     'true'),
    ('comment_auto_approve','false'),
    ('sensitive_words',     '[]');
```

---

## 4. 索引策略

| 表             | 索引                         | 说明                    |
| -------------- | ---------------------------- | ----------------------- |
| posts          | `uk_posts_slug`              | Slug 唯一，前台路由查找 |
| posts          | `idx_posts_status_published` | 首页列表查询核心索引    |
| comments       | `idx_comments_post`          | 文章评论列表            |
| comments       | `idx_comments_status`        | 后台待审核列表          |
| refresh_tokens | `idx_refresh_tokens_hash`    | Token 查找              |

**查询优化原则**：

- 列表查询走索引，避免全表扫描
- 所有软删除表索引均带 `WHERE deleted_at IS NULL` 过滤
- 当前 API 使用有上限的 `LIMIT/OFFSET` 分页（`limit <= 100`）；数据量显著增长后再迁移 Keyset Pagination
- D1 使用 prepared statement（`?` 占位符），防止 SQL 注入

---

## 5. 迁移工作流

文件按序号递增放置在 `migrations/` 目录：

```
migrations/
  0001_init_schema.sql
  0002_theme_settings.sql
  0003_seed_admin.sql
  0004_disable_known_default_admin.sql
```

通过 Wrangler CLI 执行：

```bash
# 本地开发
wrangler d1 migrations apply nami-blog --local

# 远程 preview
wrangler d1 migrations apply nami-blog-preview --remote

# 远程生产（谨慎）
wrangler d1 migrations apply nami-blog --remote
```

规则：

- 已进入远程环境的 migration 永不修改；修正必须新增 migration
- CI 在本地空库执行全量迁移验证
- 生产采用"扩展 → 应用切换 → 收缩"三步法
- D1 不支持 `ALTER TABLE DROP COLUMN`（部分版本），删除列需重建表
- 禁止通过 Cloudflare Dashboard 手工改变生产 schema

---

## 6. 备份策略

| 环境 | 策略                                 | 工具                            |
| ---- | ------------------------------------ | ------------------------------- |
| 生产 | D1 Time Travel（自动保留 30 天历史） | Cloudflare 内置                 |
| 生产 | 定期导出全量数据到本地 / Git         | `wrangler d1 export`            |
| 生产 | 重大变更前手动快照                   | `wrangler d1 export` → 本地归档 |
| 开发 | 按需导出                             | `wrangler d1 export --local`    |

导出命令：

```bash
# 导出生产数据
wrangler d1 export nami-blog --output backup_$(date +%Y%m%d).sql
```

恢复测试：每季度执行一次从备份恢复的流程验证。

## 7. 公开读模型约束

公开 API 只允许读取满足以下条件的文章：

```sql
status = 'published'
AND is_public = 1
AND deleted_at IS NULL
```

分类和标签的 `post_count` 也使用同一组条件，避免草稿、归档或软删除文章影响前台统计。

`content_html` 如果由外部内容导入，必须在写入 D1 前完成 HTML 白名单清洗；数据库层不承担 HTML 安全责任。

## 8. 管理员初始化与迁移安全

- `0003_seed_admin.sql` 不再写入固定默认密码。
- `0004_disable_known_default_admin.sql` 只会禁用仍匹配公开默认哈希的旧账号，已修改密码的账号不会匹配。
- 本地空数据库允许通过回环地址首次登录创建 `admin`，模板密码只用于本机。
- 生产空数据库允许通过 HTTPS 和 Workers 一次性 Secret 初始化；必须使用 12–128 字符的自定义密码，公开本地模板值会被拒绝。
- 初始化不会覆盖已有账号；生产首次登录改密后应删除一次性 Secret，真实密码不得进入 Git、文档或日志。
- `refresh_tokens` 保存哈希而非明文令牌；访问 token 和刷新 token 通过 `token_use` 区分。
