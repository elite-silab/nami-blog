# Nami Blog 本地开发配置
#
# 使用方法（无需敲复制命令）：
# 1. 在文件管理器或编辑器中复制本文件；
# 2. 将副本改名为 .env；
# 3. 本地使用默认端口时，直接保存即可运行。
#
# 这个文件只用于本机开发。生产环境的密钥请填写到 Cloudflare 控制台，
# 不要把真实生产密钥写进本文件或提交到 Git。

# ---------- 新手通常只需要看这里 ----------

# 本地管理后台首次登录密码
# 用户名固定为 admin；仅在本地空数据库第一次登录时创建账号。
# 首次登录后可在“后台设置 → 安全设置”中修改，修改本文件不会重置已有账号。
ADMIN_INITIAL_PASSWORD=nami-local-admin

# Web 访问的本地 Workers 地址
PUBLIC_API_URL=http://localhost:8788

# Web 本地站点地址，用于 SEO、Sitemap 和分享链接
SITE_URL=http://localhost:4322

# ---------- 本地安全密钥 ----------
# 仅供本机开发；正式环境必须在 Cloudflare 控制台换成两个互不相同的随机长字符串。
# 变量名没有 PUBLIC_ 前缀，不会进入浏览器代码。

JWT_SECRET=nami-local-only-access-secret-never-use-in-production
JWT_REFRESH_SECRET=nami-local-only-refresh-secret-never-use-in-production
